services:
  - type: web
    name: pocketbase
    env: docker
    plan: free
    branch: main
    healthCheckPath: /
    startCommand: |
      ./pocketbase superuser upsert admin@example.com MyStrongPass123
      ./pocketbase serve --http=0.0.0.0:10000
